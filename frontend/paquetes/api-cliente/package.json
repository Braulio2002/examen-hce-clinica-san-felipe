{
  "name": "@hce/api-cliente",
  "version": "1.0.0",
  "private": true,
  "description": "Cliente HTTP compartido por las zonas del microfront: interceptores JWT, tipos y contratos de la API",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --noEmit"
  },
  "dependencies": {
    "axios": "^1.7.9"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
